{% extends 'base.html.twig' %}

{% block title %}GoldTrophy{% endblock %}

{% block body %}
    <h1>GoldTrophy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gold_trophy.id }}</td>
            </tr>
            <tr>
                <th>Award</th>
                <td>{{ gold_trophy.award }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ gold_trophy.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gold_trophy_index') }}">back to list</a>

    <a href="{{ path('gold_trophy_edit', {'id': gold_trophy.id}) }}">edit</a>

    {{ include('gold_trophy/_delete_form.html.twig') }}
{% endblock %}
