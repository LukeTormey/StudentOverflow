<form method="post" action="{{ path('bronze_trophy_delete', {'id': bronze_trophy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bronze_trophy.id) }}">
    <button class="btn">Delete</button>
</form>
