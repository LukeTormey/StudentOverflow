{% extends 'base.html.twig' %}

{% block title %}Trophy{% endblock %}

{% block body %}
    <h1>Trophy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trophy.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ trophy.color }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ trophy.points }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trophy_index') }}">back to list</a>

    <a href="{{ path('trophy_edit', {'id': trophy.id}) }}">edit</a>

    {{ include('trophy/_delete_form.html.twig') }}
{% endblock %}
