{% extends 'base.html.twig' %}

{% block title %}Assignment{% endblock %}

{% block body %}
    <h1>Assignment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assignment.id }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ assignment.subject }}</td>
            </tr>
            <tr>
                <th>Deadline</th>
                <td>{{ assignment.deadline }}</td>
            </tr>
            <tr>
                <th>Wordcount</th>
                <td>{{ assignment.wordcount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('assignment_index') }}">back to list</a>

    <a href="{{ path('assignment_edit', {'id': assignment.id}) }}">edit</a>

    {{ include('assignment/_delete_form.html.twig') }}
{% endblock %}
